"use strict";
// import { Router } from 'express';
// import passport from '../utils/passport';
// import * as supportController from '../controllers/support.controller';
// const router = Router();
// router.post('/support-tickets', passport.authenticate('user-jwt', { session: false }), supportController.createTicket);
// router.get('/support-tickets', passport.authenticate('user-jwt', { session: false }), supportController.getTickets);
// export default router;
